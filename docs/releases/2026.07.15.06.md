### Privacy mask settings
- Fix: Save each granular privacy checkbox as an immediate leaf-level preference update so unchecked values are preserved without stale dashboard fields overwriting them.
- Fix: Reapply Docker, VM, and Dashboard privacy classes whenever synchronized preferences change, including updates made from another browser tab.
- Fix: Automatically replay durable pending preference changes when a runtime page restores them through its normal preference reconciliation path.
- Test: Added regressions for unchecked privacy masks, runtime preference subscriptions, immediate partial saves, and restored-outbox replay.
