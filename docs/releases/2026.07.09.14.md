## FolderView Plus 2026.07.09.14

- Fix: Docker basic view now reconciles pinned folders from the server immediately after rendering rows, so the pin badge and pinned row class recover even when the initial host render used stale or partial preferences.
