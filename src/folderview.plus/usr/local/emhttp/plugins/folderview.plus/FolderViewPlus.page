Menu="Utilities"
Title="FolderView Plus"
Icon="folder-open-o"
Type="xmenu"
Tabs="true"
Markdown="false"
---

<?php
require_once('/usr/local/emhttp/plugins/folderview.plus/langs/script.php');
?>

<script>
    folderi18n = () => {
        $('body').i18n();
    }
</script>
<link rel="stylesheet" href="<?=autov('/plugins/folderview.plus/styles/folderviewplus.css')?>">

<div class="fv-customizations-header" data-fv-section-row="1">
    <h2 data-i18n="customizations" data-fv-section="customizations">Customizations</h2>
    <div id="fv-settings-controls"></div>
</div>
<button data-i18n="file-manager" onclick="fileManager()">Open File Manager</button>
<button onclick="checkForUpdatesNow()"><i class="fa fa-refresh" aria-hidden="true"></i> Check for updates now</button>
<span id="update-check-status" class="status-line"></span>


<h2 data-i18n="docker" data-fv-section="docker">Docker</h2>
<div class="folder-table">
    <div class="folder-toolbar">
        <div class="toolbar-actions">
            <button data-i18n="[html]export-all" onclick="downloadDocker()"><i class="fa fa-download" aria-hidden="true"></i> Export all</button>
            <button data-i18n="[html]import-all" onclick="importDocker()"><i class="fa fa-upload" aria-hidden="true"></i> Import</button>
            <button onclick="restoreLatestBackup('docker')"><i class="fa fa-history" aria-hidden="true"></i> Restore latest backup</button>
            <button data-i18n="[html]clear-all" onclick="clearDocker()"><i class="fa fa-ban" aria-hidden="true"></i> Clear</button>
        </div>
        <div class="toolbar-sort">
            <div class="sort-row">
                <label class="sort-label" for="docker-sort-mode">Sort mode</label>
                <select id="docker-sort-mode" onchange="changeSortMode('docker', this.value)">
                    <option value="created">Created order</option>
                    <option value="manual">Manual (buttons)</option>
                    <option value="alpha">Name (A-Z)</option>
                </select>
                <span class="sort-hint-chip" title="Use manual mode and the up/down buttons in the first column to set custom order."><i class="fa fa-info-circle"></i> Manual uses up/down in Order column</span>
            </div>
            <input id="docker-folder-filter" type="text" placeholder="Search Docker folders" oninput="setFilterQuery('folders','docker',this.value)">
            <div class="settings-cards-grid">
                <div class="settings-mini-card">
                    <div class="settings-mini-title">Layout</div>
                    <label class="setting-toggle"><input id="docker-hide-empty-folders" type="checkbox" onchange="changeVisibilityPref('docker', 'hideEmptyFolders', this.checked)"> Hide empty folders</label>
                    <div class="setting-note">Pin folders with the star button in the Actions column.</div>
                </div>
                <div class="settings-mini-card">
                    <div class="settings-mini-title">Folder badges</div>
                    <label class="setting-toggle"><input id="docker-badge-running" type="checkbox" onchange="changeBadgePref('docker', 'running', this.checked)"> Running</label>
                    <label class="setting-toggle"><input id="docker-badge-stopped" type="checkbox" onchange="changeBadgePref('docker', 'stopped', this.checked)"> Stopped</label>
                    <label class="setting-toggle"><input id="docker-badge-updates" type="checkbox" onchange="changeBadgePref('docker', 'updates', this.checked)"> Updates</label>
                </div>
                <div class="settings-mini-card">
                    <div class="settings-mini-title">Runtime</div>
                    <label class="setting-toggle"><input id="docker-live-refresh-enabled" type="checkbox" onchange="changeRuntimePref('docker', 'liveRefreshEnabled', this.checked)"> Live auto-refresh</label>
                    <label id="docker-live-refresh-seconds-row" class="setting-inline-number runtime-dependent">
                        <span>Interval (sec)</span>
                        <input id="docker-live-refresh-seconds" type="number" min="10" max="300" step="1" onchange="changeRuntimePref('docker', 'liveRefreshSeconds', this.value)">
                    </label>
                    <label class="setting-toggle"><input id="docker-performance-mode" type="checkbox" onchange="changeRuntimePref('docker', 'performanceMode', this.checked)"> Performance mode</label>
                    <label class="setting-toggle"><input id="docker-lazy-preview-enabled" type="checkbox" onchange="changeRuntimePref('docker', 'lazyPreviewEnabled', this.checked)"> Lazy previews</label>
                    <label id="docker-lazy-preview-threshold-row" class="setting-inline-number runtime-dependent">
                        <span>Lazy threshold</span>
                        <input id="docker-lazy-preview-threshold" type="number" min="10" max="200" step="1" onchange="changeRuntimePref('docker', 'lazyPreviewThreshold', this.value)">
                    </label>
                </div>
            </div>
        </div>
    </div>
    <div class="table-wrap">
        <table>
            <thead>
                <tr>
                    <th>Order</th>
                    <th data-i18n="el-id">
                        ID
                    </th>
                    <th data-i18n="el-name">
                        Name
                    </th>
                    <th data-i18n="el-actions">
                        Actions
                    </th>
                </tr>
            </thead>
            <tbody id="docker">
                
            </tbody>
        </table>
    </div>
</div>
<hr>

<h2 data-i18n="vms" data-fv-section="vms">VMs</h2>
<div class="folder-table">
    <div class="folder-toolbar">
        <div class="toolbar-actions">
            <button data-i18n="[html]export-all" onclick="downloadVm()"><i class="fa fa-download" aria-hidden="true"></i> Export all</button>
            <button data-i18n="[html]import-all" onclick="importVm()"><i class="fa fa-upload" aria-hidden="true"></i> Import</button>
            <button onclick="restoreLatestBackup('vm')"><i class="fa fa-history" aria-hidden="true"></i> Restore latest backup</button>
            <button data-i18n="[html]clear-all" onclick="clearVm()"><i class="fa fa-ban" aria-hidden="true"></i> Clear</button>
        </div>
        <div class="toolbar-sort">
            <div class="sort-row">
                <label class="sort-label" for="vm-sort-mode">Sort mode</label>
                <select id="vm-sort-mode" onchange="changeSortMode('vm', this.value)">
                    <option value="created">Created order</option>
                    <option value="manual">Manual (buttons)</option>
                    <option value="alpha">Name (A-Z)</option>
                </select>
                <span class="sort-hint-chip" title="Use manual mode and the up/down buttons in the first column to set custom order."><i class="fa fa-info-circle"></i> Manual uses up/down in Order column</span>
            </div>
            <input id="vm-folder-filter" type="text" placeholder="Search VM folders" oninput="setFilterQuery('folders','vm',this.value)">
            <div class="settings-cards-grid">
                <div class="settings-mini-card">
                    <div class="settings-mini-title">Layout</div>
                    <label class="setting-toggle"><input id="vm-hide-empty-folders" type="checkbox" onchange="changeVisibilityPref('vm', 'hideEmptyFolders', this.checked)"> Hide empty folders</label>
                    <div class="setting-note">Pin folders with the star button in the Actions column.</div>
                </div>
                <div class="settings-mini-card">
                    <div class="settings-mini-title">Folder badges</div>
                    <label class="setting-toggle"><input id="vm-badge-running" type="checkbox" onchange="changeBadgePref('vm', 'running', this.checked)"> Running</label>
                    <label class="setting-toggle"><input id="vm-badge-stopped" type="checkbox" onchange="changeBadgePref('vm', 'stopped', this.checked)"> Stopped</label>
                </div>
                <div class="settings-mini-card">
                    <div class="settings-mini-title">Runtime</div>
                    <label class="setting-toggle"><input id="vm-live-refresh-enabled" type="checkbox" onchange="changeRuntimePref('vm', 'liveRefreshEnabled', this.checked)"> Live auto-refresh</label>
                    <label id="vm-live-refresh-seconds-row" class="setting-inline-number runtime-dependent">
                        <span>Interval (sec)</span>
                        <input id="vm-live-refresh-seconds" type="number" min="10" max="300" step="1" onchange="changeRuntimePref('vm', 'liveRefreshSeconds', this.value)">
                    </label>
                    <label class="setting-toggle"><input id="vm-performance-mode" type="checkbox" onchange="changeRuntimePref('vm', 'performanceMode', this.checked)"> Performance mode</label>
                    <label class="setting-toggle"><input id="vm-lazy-preview-enabled" type="checkbox" onchange="changeRuntimePref('vm', 'lazyPreviewEnabled', this.checked)"> Lazy previews</label>
                    <label id="vm-lazy-preview-threshold-row" class="setting-inline-number runtime-dependent">
                        <span>Lazy threshold</span>
                        <input id="vm-lazy-preview-threshold" type="number" min="10" max="200" step="1" onchange="changeRuntimePref('vm', 'lazyPreviewThreshold', this.value)">
                    </label>
                </div>
            </div>
        </div>
    </div>
    <div class="table-wrap">
        <table>
            <thead>
                <tr>
                    <th>Order</th>
                    <th data-i18n="el-id">
                        ID
                    </th>
                    <th data-i18n="el-name">
                        Name
                    </th>
                    <th data-i18n="el-actions">
                        Actions
                    </th>
                </tr>
            </thead>
            <tbody id="vms">
        
            </tbody>
        </table>
    </div>
</div>
<hr>

<h2 data-fv-section="auto-assignment" data-fv-advanced="1">Auto-assignment rules</h2>
<div class="rules-bottom-grid">
    <div class="rules-panel">
        <div class="rules-header">
            <h3>Docker rules</h3>
            <span class="rules-help">Automatically place new containers into folders.</span>
        </div>
        <div class="rules-editor">
            <select id="docker-rule-folder"></select>
            <select id="docker-rule-effect">
                <option value="include">Include</option>
                <option value="exclude">Exclude</option>
            </select>
            <select id="docker-rule-kind" onchange="toggleRuleKindFields('docker')">
                <option value="name_regex">Name regex</option>
                <option value="label">Label equals</option>
                <option value="label_contains">Label contains</option>
                <option value="label_starts_with">Label starts with</option>
                <option value="image_regex">Image regex</option>
                <option value="compose_project_regex">Compose project regex</option>
            </select>
            <input id="docker-rule-pattern" type="text" placeholder="Regex pattern (example: ^media-)">
            <input id="docker-rule-label-key" type="text" placeholder="Label key (example: com.example.stack)" style="display:none;">
            <input id="docker-rule-label-value" type="text" placeholder="Label value (optional, blank means any value)" style="display:none;">
            <button class="rules-add-btn" onclick="addAutoRule('docker')"><i class="fa fa-plus"></i> Add rule</button>
        </div>
        <div class="rule-tester">
            <input id="docker-rule-test-name" type="text" placeholder="Test name (example: media-server)">
            <input id="docker-rule-test-label-key" type="text" placeholder="Test label key (optional)">
            <input id="docker-rule-test-label-value" type="text" placeholder="Test label value (optional)">
            <input id="docker-rule-test-image" type="text" placeholder="Test image (optional, example: linuxserver/sonarr)">
            <input id="docker-rule-test-compose" type="text" placeholder="Test compose project (optional)">
            <button onclick="testAutoRule('docker')"><i class="fa fa-flask"></i> Test rule priority</button>
            <button onclick="runRuleSimulator('docker')"><i class="fa fa-list-alt"></i> Simulate all items</button>
            <div id="docker-rule-test-output" class="rule-test-output"></div>
        </div>
        <input id="docker-rules-filter" type="text" placeholder="Search Docker rules" oninput="setFilterQuery('rules','docker',this.value)">
        <div class="selection-actions">
            <button onclick="bulkRuleAction('docker','enable')"><i class="fa fa-eye"></i> Enable selected</button>
            <button onclick="bulkRuleAction('docker','disable')"><i class="fa fa-eye-slash"></i> Disable selected</button>
            <button onclick="bulkRuleAction('docker','delete')"><i class="fa fa-trash"></i> Delete selected</button>
            <button onclick="bulkRuleAction('docker','export')"><i class="fa fa-download"></i> Export selected</button>
        </div>
        <pre id="docker-rule-sim-output" class="diagnostics-output compact-output">Run simulator to preview assignment results for all Docker items.</pre>
        <table class="rules-table auto-rules-table">
            <thead>
                <tr>
                    <th><input type="checkbox" id="docker-rules-select-all" onchange="toggleAllRuleSelections('docker', this.checked)"></th>
                    <th>Priority</th>
                    <th>Folder</th>
                    <th>Match</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody id="docker-rules"></tbody>
        </table>
    </div>

    <div class="rules-panel">
        <div class="rules-header">
            <h3>VM rules</h3>
            <span class="rules-help">Automatically place new VMs into folders.</span>
        </div>
        <div class="rules-editor">
            <select id="vm-rule-folder"></select>
            <select id="vm-rule-effect">
                <option value="include">Include</option>
                <option value="exclude">Exclude</option>
            </select>
            <select id="vm-rule-kind">
                <option value="name_regex">Name regex</option>
            </select>
            <input id="vm-rule-pattern" type="text" placeholder="Regex pattern (example: ^prod-)">
            <button class="rules-add-btn" onclick="addAutoRule('vm')"><i class="fa fa-plus"></i> Add rule</button>
        </div>
        <div class="rule-tester">
            <input id="vm-rule-test-name" type="text" placeholder="Test VM name (example: prod-db)">
            <button onclick="testAutoRule('vm')"><i class="fa fa-flask"></i> Test rule priority</button>
            <button onclick="runRuleSimulator('vm')"><i class="fa fa-list-alt"></i> Simulate all items</button>
            <div id="vm-rule-test-output" class="rule-test-output"></div>
        </div>
        <input id="vm-rules-filter" type="text" placeholder="Search VM rules" oninput="setFilterQuery('rules','vm',this.value)">
        <div class="selection-actions">
            <button onclick="bulkRuleAction('vm','enable')"><i class="fa fa-eye"></i> Enable selected</button>
            <button onclick="bulkRuleAction('vm','disable')"><i class="fa fa-eye-slash"></i> Disable selected</button>
            <button onclick="bulkRuleAction('vm','delete')"><i class="fa fa-trash"></i> Delete selected</button>
            <button onclick="bulkRuleAction('vm','export')"><i class="fa fa-download"></i> Export selected</button>
        </div>
        <pre id="vm-rule-sim-output" class="diagnostics-output compact-output">Run simulator to preview assignment results for all VM items.</pre>
        <table class="rules-table auto-rules-table">
            <thead>
                <tr>
                    <th><input type="checkbox" id="vm-rules-select-all" onchange="toggleAllRuleSelections('vm', this.checked)"></th>
                    <th>Priority</th>
                    <th>Folder</th>
                    <th>Match</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody id="vm-rules"></tbody>
        </table>
    </div>
</div>
<hr>

<h2 data-fv-section="bulk-assignment" data-fv-advanced="1">Bulk assignment</h2>
<div class="bulk-assign-grid">
    <div class="rules-panel">
        <div class="rules-header">
            <h3>Docker bulk assignment</h3>
            <span class="rules-help">Select multiple containers and assign them to one folder in a single action.</span>
        </div>
        <div class="bulk-row">
            <select id="docker-bulk-folder"></select>
            <button onclick="assignSelectedItems('docker')"><i class="fa fa-share-square-o"></i> Assign selected</button>
        </div>
        <select id="docker-bulk-items" multiple size="10"></select>
    </div>

    <div class="rules-panel">
        <div class="rules-header">
            <h3>VM bulk assignment</h3>
            <span class="rules-help">Select multiple VMs and assign them to one folder in a single action.</span>
        </div>
        <div class="bulk-row">
            <select id="vm-bulk-folder"></select>
            <button onclick="assignSelectedItems('vm')"><i class="fa fa-share-square-o"></i> Assign selected</button>
        </div>
        <select id="vm-bulk-items" multiple size="10"></select>
    </div>
</div>
<hr>

<h2 data-fv-section="runtime-actions" data-fv-advanced="1">Folder runtime actions</h2>
<div class="bulk-assign-grid">
    <div class="rules-panel">
        <div class="rules-header">
            <h3>Docker folder actions</h3>
            <span class="rules-help">Preview and apply Start/Stop/Pause/Resume for items currently in a folder.</span>
        </div>
        <div class="runtime-action-grid">
            <select id="docker-runtime-folder"></select>
            <select id="docker-runtime-action">
                <option value="start">Start</option>
                <option value="stop">Stop</option>
                <option value="pause">Pause</option>
                <option value="resume">Resume</option>
            </select>
            <button onclick="previewFolderRuntimeAction('docker')"><i class="fa fa-list-alt"></i> Preview</button>
            <button onclick="applyFolderRuntimeAction('docker')"><i class="fa fa-play"></i> Apply action</button>
        </div>
        <pre id="docker-runtime-preview-output" class="diagnostics-output compact-output">Preview a runtime action to see exactly which containers will be changed.</pre>
    </div>

    <div class="rules-panel">
        <div class="rules-header">
            <h3>VM folder actions</h3>
            <span class="rules-help">Preview and apply Start/Stop/Pause/Resume for VMs currently in a folder.</span>
        </div>
        <div class="runtime-action-grid">
            <select id="vm-runtime-folder"></select>
            <select id="vm-runtime-action">
                <option value="start">Start</option>
                <option value="stop">Stop</option>
                <option value="pause">Pause</option>
                <option value="resume">Resume</option>
            </select>
            <button onclick="previewFolderRuntimeAction('vm')"><i class="fa fa-list-alt"></i> Preview</button>
            <button onclick="applyFolderRuntimeAction('vm')"><i class="fa fa-play"></i> Apply action</button>
        </div>
        <pre id="vm-runtime-preview-output" class="diagnostics-output compact-output">Preview a runtime action to see exactly which VMs will be changed.</pre>
    </div>
</div>
<hr>

<h2 data-fv-section="backups" data-fv-advanced="1">Backups</h2>
<div class="backup-grid">
    <div class="rules-panel">
        <div class="rules-header">
            <h3>Docker backups</h3>
            <span class="rules-help">Create, restore, download, or delete backup snapshots.</span>
        </div>
        <div class="backup-actions">
            <button onclick="createManualBackup('docker')"><i class="fa fa-camera"></i> Create backup now</button>
            <button onclick="restoreLatestBackup('docker')"><i class="fa fa-history"></i> Restore latest</button>
            <button onclick="refreshBackups('docker')"><i class="fa fa-refresh"></i> Refresh list</button>
            <button onclick="runScheduledBackupNow('docker')"><i class="fa fa-clock-o"></i> Run scheduler now</button>
        </div>
        <input id="docker-backups-filter" type="text" placeholder="Search Docker backups" oninput="setFilterQuery('backups','docker',this.value)">
        <div class="schedule-row">
            <label><input id="docker-backup-schedule-enabled" type="checkbox" onchange="changeBackupSchedulePref('docker', 'enabled', this.checked)"> Scheduled backups</label>
            <label>Interval (hours): <input id="docker-backup-interval-hours" type="number" min="1" max="168" step="1" onchange="changeBackupSchedulePref('docker', 'intervalHours', this.value)"></label>
            <label>Retention: <input id="docker-backup-retention" type="number" min="1" max="200" step="1" onchange="changeBackupSchedulePref('docker', 'retention', this.value)"></label>
            <span id="docker-backup-last-run" class="schedule-hint"></span>
        </div>
        <table class="rules-table">
            <thead>
                <tr>
                    <th>Created</th>
                    <th>Reason</th>
                    <th>Folders</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody id="docker-backups"></tbody>
        </table>
    </div>

    <div class="rules-panel">
        <div class="rules-header">
            <h3>VM backups</h3>
            <span class="rules-help">Create, restore, download, or delete backup snapshots.</span>
        </div>
        <div class="backup-actions">
            <button onclick="createManualBackup('vm')"><i class="fa fa-camera"></i> Create backup now</button>
            <button onclick="restoreLatestBackup('vm')"><i class="fa fa-history"></i> Restore latest</button>
            <button onclick="refreshBackups('vm')"><i class="fa fa-refresh"></i> Refresh list</button>
            <button onclick="runScheduledBackupNow('vm')"><i class="fa fa-clock-o"></i> Run scheduler now</button>
        </div>
        <input id="vm-backups-filter" type="text" placeholder="Search VM backups" oninput="setFilterQuery('backups','vm',this.value)">
        <div class="schedule-row">
            <label><input id="vm-backup-schedule-enabled" type="checkbox" onchange="changeBackupSchedulePref('vm', 'enabled', this.checked)"> Scheduled backups</label>
            <label>Interval (hours): <input id="vm-backup-interval-hours" type="number" min="1" max="168" step="1" onchange="changeBackupSchedulePref('vm', 'intervalHours', this.value)"></label>
            <label>Retention: <input id="vm-backup-retention" type="number" min="1" max="200" step="1" onchange="changeBackupSchedulePref('vm', 'retention', this.value)"></label>
            <span id="vm-backup-last-run" class="schedule-hint"></span>
        </div>
        <table class="rules-table">
            <thead>
                <tr>
                    <th>Created</th>
                    <th>Reason</th>
                    <th>Folders</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody id="vm-backups"></tbody>
        </table>
    </div>
</div>
<hr>

<h2 data-fv-section="folder-templates" data-fv-advanced="1">Folder templates</h2>
<div class="template-grid">
    <div class="rules-panel">
        <div class="rules-header">
            <h3>Docker templates</h3>
            <span class="rules-help">Save folder look/behavior presets and apply them to any folder.</span>
        </div>
        <div class="template-controls">
            <select id="docker-template-source-folder"></select>
            <input id="docker-template-name" type="text" placeholder="Template name">
            <button onclick="createTemplateFromFolder('docker')"><i class="fa fa-save"></i> Save template from folder</button>
        </div>
        <input id="docker-templates-filter" type="text" placeholder="Search Docker templates" oninput="setFilterQuery('templates','docker',this.value)">
        <div class="selection-actions">
            <button onclick="bulkTemplateAction('docker','delete')"><i class="fa fa-trash"></i> Delete selected</button>
            <button onclick="bulkTemplateAction('docker','export')"><i class="fa fa-download"></i> Export selected</button>
        </div>
        <table class="rules-table">
            <thead>
                <tr>
                    <th><input type="checkbox" id="docker-templates-select-all" onchange="toggleAllTemplateSelections('docker', this.checked)"></th>
                    <th>Name</th>
                    <th>Updated</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody id="docker-templates"></tbody>
        </table>
    </div>

    <div class="rules-panel">
        <div class="rules-header">
            <h3>VM templates</h3>
            <span class="rules-help">Save VM folder presets and apply them without manual reconfiguration.</span>
        </div>
        <div class="template-controls">
            <select id="vm-template-source-folder"></select>
            <input id="vm-template-name" type="text" placeholder="Template name">
            <button onclick="createTemplateFromFolder('vm')"><i class="fa fa-save"></i> Save template from folder</button>
        </div>
        <input id="vm-templates-filter" type="text" placeholder="Search VM templates" oninput="setFilterQuery('templates','vm',this.value)">
        <div class="selection-actions">
            <button onclick="bulkTemplateAction('vm','delete')"><i class="fa fa-trash"></i> Delete selected</button>
            <button onclick="bulkTemplateAction('vm','export')"><i class="fa fa-download"></i> Export selected</button>
        </div>
        <table class="rules-table">
            <thead>
                <tr>
                    <th><input type="checkbox" id="vm-templates-select-all" onchange="toggleAllTemplateSelections('vm', this.checked)"></th>
                    <th>Name</th>
                    <th>Updated</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody id="vm-templates"></tbody>
        </table>
    </div>
</div>
<hr>

<h2 data-fv-section="change-history" data-fv-advanced="1">Change history and undo</h2>
<div class="rules-panel">
    <div class="rules-header">
        <h3>Recent changes</h3>
        <span class="rules-help">Shows recent config actions from diagnostics history and allows one-click undo to the latest backup transaction.</span>
    </div>
    <div class="backup-actions">
        <button onclick="refreshChangeHistory()"><i class="fa fa-refresh"></i> Refresh history</button>
        <button onclick="undoLatestChange('docker')"><i class="fa fa-undo"></i> Undo latest Docker change</button>
        <button onclick="undoLatestChange('vm')"><i class="fa fa-undo"></i> Undo latest VM change</button>
    </div>
    <pre id="change-history-output" class="diagnostics-output compact-output">Refresh history to view recent changes.</pre>
</div>
<hr>

<h2 data-fv-section="diagnostics" data-fv-advanced="1">Diagnostics</h2>
<div class="rules-panel">
    <div class="rules-header">
        <h3>Health checks and quick repair</h3>
        <span class="rules-help">Check plugin health, then run one-click repair actions when needed.</span>
    </div>
    <div class="backup-actions">
        <button onclick="runDiagnostics()"><i class="fa fa-stethoscope"></i> Run health check</button>
        <button onclick="repairDiagnostics('sync_docker_order')"><i class="fa fa-sort"></i> Rebuild Docker order index</button>
        <button onclick="repairDiagnostics('normalize_prefs')"><i class="fa fa-wrench"></i> Validate and normalize prefs</button>
        <button onclick="repairDiagnostics('repair_paths')"><i class="fa fa-folder-open"></i> Repair plugin paths</button>
        <button onclick="exportDiagnostics()"><i class="fa fa-download"></i> Export diagnostics JSON</button>
        <button onclick="exportSupportBundle()"><i class="fa fa-life-ring"></i> Export support bundle</button>
        <button onclick="copyIssueReport()"><i class="fa fa-clipboard"></i> Copy issue report</button>
    </div>
    <pre id="diagnostics-output" class="diagnostics-output">Run health check to view diagnostics.</pre>
</div>
<hr>

<h2 data-fv-section="conflict-inspector" data-fv-advanced="1">Conflict inspector</h2>
<div class="rules-panel">
    <div class="rules-header">
        <h3>Folder membership conflict report</h3>
        <span class="rules-help">Shows why each item is assigned (manual/regex/label/rule), including overlaps and blocked rules.</span>
    </div>
    <div class="backup-actions">
        <button onclick="runConflictInspector('docker')"><i class="fa fa-search"></i> Scan Docker conflicts</button>
        <button onclick="runConflictInspector('vm')"><i class="fa fa-search"></i> Scan VM conflicts</button>
    </div>
    <pre id="conflict-output" class="diagnostics-output">Run a conflict scan to view results.</pre>
</div>

<div id="import-preview-dialog" style="display:none;">
    <p><strong>Import summary</strong></p>
    <div id="import-preview-meta" class="preview-meta"></div>
    <div id="import-preview-result" class="preview-result"></div>
    <div id="import-preview-diff"></div>
    <label for="import-mode-select">Import mode</label>
    <select id="import-mode-select">
        <option value="merge">Merge</option>
        <option value="replace">Replace</option>
        <option value="skip">Skip existing</option>
    </select>
    <div id="import-preview-selection" class="import-selection"></div>
    <textarea id="import-preview-text" readonly></textarea>
</div>

<div id="fv-settings-action-bar"></div>

<script src="<?=autov('/plugins/folderview.plus/scripts/folderviewplus.utils.js')?>"></script>
<script src="<?=autov('/plugins/folderview.plus/scripts/folderviewplus.js')?>"></script>
