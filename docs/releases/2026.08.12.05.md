- UI/UX: Load the Docker autostart sequence automatically whenever the Start Order tab is opened while keeping Preview order available as an explicit refresh action.
- Performance: Hydrate the preview only once per tab visit, then permit a fresh automatic request after leaving and reopening Start Order.
- UI/UX: Keep the active-order notice aligned to the top controls row and size it to its content instead of stretching into an oversized panel with blank space.
- Test: Cover preview activation and re-entry, the manual refresh contract, compact notice geometry, responsive overflow, and runtime performance budgets.
