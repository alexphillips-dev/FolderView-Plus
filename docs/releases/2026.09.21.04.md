- Fix: Prevent the Standard performance profile label in Settings from creating a false localization advisory. Docker and VM summaries now use the shared translation catalog available on the Settings page in every supported language.
- Regression guard: Require zero missing translation keys in startup benchmarks, and verify all performance profiles across all 27 locales while retaining warnings for genuine localization failures.
- Compatibility: Hard-refresh the Unraid webGUI after updating, then run the health check again. Missing-key advisories belong to the current page session and clear when the corrected page loads.
