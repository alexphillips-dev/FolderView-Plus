## FolderView Plus 2026.08.11.01

This development build establishes enforceable architecture boundaries for the staged monolith-reduction program without changing plugin runtime behavior.

### Architecture boundaries

- Quality: Record ownership, consumers, dependencies, state models, lifecycle responsibility, and compatibility globals for existing contracted modules and primary browser/server entrypoints.
- Quality: Replace permissive line ceilings with 38 exact, non-increasing budget histories and explicit reduction targets for primary entrypoints and oversized first-party modules.
- Quality: Freeze the current browser-global, inline-action, and Docker/VM/Dashboard function-name overlap baselines so future coupling cannot grow silently.
- Quality: Require newly extracted browser and PHP modules to enter a reviewed contract instead of expanding the legacy uncontracted inventory.

### Load-order protection

- Quality: Expand load-order validation from Settings alone to all five plugin pages, shared localization/custom bootstraps, and the Folder Editor dynamic runtime queue.
- Quality: Validate 201 ordered script references and verify that every referenced plugin script and page entrypoint exists.
- Quality: Add focused regression tests for page-graph coverage, boundary metadata, ratcheting histories, and legacy module inventory fingerprints.
