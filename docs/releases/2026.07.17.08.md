- UX: Docker startup now leaves Unraid's native container rows visible while FolderView Plus resolves its folder data and preferences, then converts those rows into the completed folder view without an intermediate blank or cloned overlay.
- Fix: Removed the temporary Docker render snapshot, hidden tbody staging state, and related cleanup paths introduced during the cascading-chevron investigation.
- Performance: Folder rows are now inserted in one uninterrupted synchronous DOM commit, matching FolderView3's paint behavior and preventing the browser from displaying partial folder or chevron states between animation frames.
- Quality: Replaced the former yield/staging contracts with regressions that require native-first hook ordering, no render snapshot CSS or JavaScript, and no asynchronous yield inside either folder-construction loop.
