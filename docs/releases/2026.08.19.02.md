- Diagnostics: Retain up to five bounded Docker-page session summaries with start/end time, duration, exit reason, load/render/request counts, reload cadence, native busy-cycle recovery, and restored-folder evidence.
- Diagnostics: Classify full Docker reloads by fixed privacy-safe sources and derive `healthy`, `suspected`, or `confirmed` refresh-loop verdicts with reference `FVPLUS-DKR-REFRESH-001`.
- Reliability: Record API identity differences as aggregate counts only and keep Unraid's native configuration revision authoritative without requesting a host reload from the mismatch path.
- Diagnostics: Preserve detailed browser-error evidence for the current session and previous 30 days while reducing older retained errors to category counts.
- Maintenance: Extend sanitized support-bundle comparison to highlight refresh-loop, busy-cycle, API-mismatch, mutation, and transfer regressions.
- Privacy: Keep Docker refresh diagnostics bounded and free of container names, IDs, raw API payloads, GraphQL queries, URLs, tokens, cookies, process lists, syslogs, and call stacks.
