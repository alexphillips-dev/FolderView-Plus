- Fix: Restore Docker folder boxes for folders created through the Add Folder wizard when Unraid renders container rows with short container IDs instead of container names.
- Reliability: Resolve Docker row identities through the normalized runtime container model and preserve deterministic first-folder ownership when rules overlap.
- Diagnostics: Add privacy-safe aggregate folder-grouping evidence for host-row resolution, claimed members, missing members, rendered folders, and hide-empty removals.
- Test: Reproduce the reported 25-folder and 18-container layout with manual, regex, label, and advanced-rule membership in unit and browser fixtures.
