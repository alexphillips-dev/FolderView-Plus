# FolderView Plus 2026.07.19.03

- Feature: Expand the release-note taxonomy with dedicated Reliability, Privacy, Accessibility, Localization, and Diagnostics categories while preserving every previously supported tag.
- Quality: Centralize all 18 allowed tags and their update-summary mappings in one packaged JSON contract shared by release validation and the installed plugin.
- Reliability: Make the release guard and automatic notes builder consume the shared contract so an accepted tag cannot drift between packaging paths.
- UX: Make the installed update-notes endpoint and browser headline formatter use the same canonical tag list instead of separate regular expressions.
- Compatibility: Continue grouping detailed tags into the existing Feature, Bug Fix, Security, Performance, UI/UX, Maintenance, and Mixed update summary badges.
- Test: Add contract, parser, classification, helper, and temporary release-flow coverage for the expanded category system.
