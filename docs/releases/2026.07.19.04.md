# FolderView Plus 2026.07.19.04

- UI/UX: Replace the oversized folder More popup with a compact, single-column action sheet shared by Docker and VM folders.
- UI/UX: Add a concise folder header, live member-state chips, progressive folder details, and a clearly separated delete action without duplicating the table's Edit and Pin controls.
- Feature: Consolidate Move to root, Move under, and Tree move into one Move folder flow that supports root, inside, before, and after destinations.
- Feature: Make branch controls context-aware so only the applicable Collapse or Expand action and Pin or Unpin entire branch action is shown.
- Feature: Consolidate folder and branch exports into one context-aware export action while retaining guided import into the selected folder.
- Accessibility: Add dialog semantics, initial focus, keyboard focus trapping, Escape dismissal, backdrop dismissal, focus restoration, and descriptive labels.
- UI/UX: Add a responsive safe-area-aware mobile bottom sheet with contained scrolling and theme-token styling that avoids the previous orange-filled action buttons.
- Quality: Remove the retired VM-only details drawer, redundant row-level diagnostics actions, stale drawer state, and obsolete popup styling; tree integrity tools remain in Advanced settings.
- Test: Replace legacy SweetAlert and VM drawer contracts with regression coverage for the unified action sheet, context-aware actions, responsive scrolling, theme tokens, and keyboard behavior.
