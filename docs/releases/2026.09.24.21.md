- UX: Redesign Advanced > Operations with side-by-side folder actions and template library cards, prominent Docker and VM tabs, an always-visible action preview, and a visible template creation form.
- Feature: Search saved templates by name. The empty library's creation shortcut focuses the template name field, and a newly saved template is selected in the library.
- Fix: Keep Apply action visible while still requiring a current preview with eligible items before a folder action can run.
- Compatibility: Hard-refresh the Unraid webGUI after updating to load the revised Settings styles and scripts.
