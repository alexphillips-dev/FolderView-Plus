- Fix: Docker View-menu transitions now rebuild Unraid's native container rows before applying the selected FolderView mode, preventing repeated switches from appending duplicate folder rows.
- UX: Removed Tree Explorer and Orbit from the Docker View menu and the Settings runtime selector, leaving FolderView, Host list, and Command as the supported Docker page views.
- Maintenance: Removed the retired Tree Explorer and Orbit JavaScript modules, stylesheets, page assets, privacy selectors, translations, and runtime integration paths from the plugin package.
- Compatibility: Existing preferences saved with either removed view now normalize safely to FolderView on both the client and server.
- Quality: Added regression coverage for repeated FolderView/Host transitions, legacy preference fallback, supported-view surfaces, and the absence of retired assets from the package.
