- Fix: Prevent Compact Matrix from stretching Docker and VM Dashboard widgets beyond the screen under Unraid's native table styles. Folder cards wrap into their columns and the view controls remain accessible without horizontal scrolling. Hard-refresh the webGUI after updating.
