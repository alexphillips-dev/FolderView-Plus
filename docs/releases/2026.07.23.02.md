- Diagnostics: Added privacy-safe Dashboard visual snapshots that record viewport, orientation, touch capability, browser scale, rendered card geometry, grid-column agreement, and overflow verdicts for Docker and VM widgets.
- UX: Added a `Capture layout diagnostics` action to Dashboard View options plus a guided Settings workflow and freshness/environment warnings in the support-bundle preview.
- Diagnostics: Split support findings into layout/rendering, configuration, runtime/request, storage, custom-icon, theme, localization, and update domains so unrelated warnings no longer obscure visual evidence.
- Privacy: Kept Dashboard captures bounded to 12 distinct snapshots per widget and eight sampled problem labels, using per-page salted fingerprints and length buckets instead of screenshots, DOM dumps, or raw names.
- Localization: Added translated Dashboard-capture and diagnostic-domain messages across all 18 supported locales and taught the catalog builder to synchronize missing explicit namespace keys with placeholder validation.
- Quality: Added unit, integration, browser, portrait/landscape viewport, privacy, stale-version, and support-preview coverage for the complete capture-to-export workflow.
