# FolderView Plus 2026.07.25.02

This dev follow-up closes two Linux CI findings discovered while monitoring the full `2026.07.25.01` publication.

## Release integrity

- Reliability: Make archive provenance content-addressed so the checked-in package can be reproduced byte for byte from its containing commit without a self-referential Git SHA.
- Reliability: Require the reproducibility guard to compare both isolated builds directly with the repository archive.

## Continuous integration

- Fix: Install lockfile-pinned Node dependencies before the node test lane so the enforced `c8` coverage command is available on clean GitHub runners.
- Regression guard: Verify that the node test job retains its dependency-install step.
