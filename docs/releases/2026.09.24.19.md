- UX: Keep Logs rows neutral while retaining colored status icons and labels for complete, attention, issue, and information entries.
- Fix: Show useful event details beside server log actions, including folder change counts, backup folder counts, and partial action outcomes. Existing recent server entries gain details when refreshed, without adding raw names or paths to the browser log.
- Compatibility: Hard-refresh the Unraid webGUI after updating to load the revised Settings styles and scripts.
