- Fix: Advanced > Logs now keeps up to 100 entries in the current browser for 30 days, so recent activity survives switching tabs, leaving Settings, and reopening the page. Entries older than 30 days are removed when the history is read.
- UX: Clear also removes the saved browser history. Clearing browser site data removes it as well; history is not shared between browsers or devices.
- Compatibility: Hard-refresh the Unraid webGUI after updating to load the revised Settings script and translated description.
