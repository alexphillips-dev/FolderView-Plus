- Feature: Show a compact Update Available link beside the FolderView Plus Settings identity when the stable manifest reports a newer plugin version, with one-click navigation to the Unraid Plugins page.
- UI/UX: Set both the Plugin settings subtitle and update label to 1.3rem, render the available-update label in bold with an inline SVG download icon, and preserve responsive wrapping across desktop and mobile layouts.
- Reliability: Run update detection only after the Settings surface is ready and keep the indicator hidden when no update is available or the optional remote check fails.
- Localization: Ship the update label and accessible Plugins-page guidance across all 27 supported locales with complete catalog coverage.
- Test: Cover the conditional link contract, accessible SVG and destination, requested typography, localized catalogs, CSP inventory, theme-safe styling, and browser-fixture overflow behavior.
