- UX: Refreshed the Basic Settings workspace for Docker and VM folder groups with visible search, filter, sort, and add controls while keeping the existing Docker and VMs section headers.
- UX: Added summary cards for each Basic Settings section so total groups, health counts, empty groups, and rule totals are visible before scanning the folder list.
- UX: Updated the folder rows with a cleaner grouped layout, larger folder identity area, compact health badges, pin switches, and simplified row actions.
- UX: Replaced up and down reorder buttons with a drag handle for same-level manual ordering while keeping the existing Tree move flow for nesting and cross-level moves.
- Quality: Updated Settings binding coverage for the moved Basic search and sort controls.
