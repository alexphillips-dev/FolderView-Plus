- Performance: Reduce Docker startup work by centering the completed folder tree once instead of repeatedly measuring every folder as each row is inserted. Folder membership, nesting, and existing expand/collapse behavior are preserved.
- Diagnostics: Include aggregate Docker startup-stage timings in support bundles, covering provider preparation, runtime loading, data preparation, folder rendering, and deferred detail application without recording container or folder identities.
- Regression guard: Add a synthetic 23-folder, seven-child-folder, 51-container workload with repeated cold/warm measurements, exact membership and hierarchy checks, and a measured baseline taken before the optimization. Existing stress-test baselines remain enforced.
- Compatibility: Hard-refresh the Unraid webGUI after updating. Docker support-bundle milestones now start at the earlier bootstrap stage; compare named operation durations when reviewing older bundles. Synthetic benchmark results do not measure your server's PHP or network latency.
