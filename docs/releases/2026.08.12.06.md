- Fix: Detect Docker autostart from the lightweight runtime rows used by Settings so the Start Order summary no longer reports zero enabled containers.
- Reliability: Preserve every peer container's actual autostart state when toggling one container instead of treating lightweight rows as disabled.
- Compatibility: Keep support for full Docker detail rows while normalizing top-level `autostart` and `autoStart` state representations.
- Test: Exercise the production lightweight payload in unit and Chromium fixtures, including visible totals, disabled classification, peer-state mutation preservation, and full-payload compatibility.
