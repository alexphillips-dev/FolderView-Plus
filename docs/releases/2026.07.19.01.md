# FolderView Plus 2026.07.19.01

- Fix: Restore Docker container icons and members in collapsed folder previews when Performance mode is enabled. Performance mode no longer rewrites the configured preview style to `None` at render time.
- Fix: Preserve configured collapsed previews on the VM page and Dashboard as well, while retaining reduced animation, slower refresh cadence, and expansion limits for large installations.
- Test: Add regression coverage proving Performance mode cannot silently disable collapsed previews on any runtime surface.
