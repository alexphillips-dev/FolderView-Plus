## FolderView Plus 2026.07.09.08

- Fix: Docker and VM runtime pin actions now reject failed preference saves instead of leaving an optimistic pinned state behind.
- Fix: Runtime pin saves now read preferences back after saving and verify the pinned-folder list persisted before keeping the row pinned.
- Fix: Recent runtime pin changes are protected from stale in-flight render payloads that can arrive after a pin click.
