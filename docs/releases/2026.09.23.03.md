- Fix: Restore the stable release's button typography and component styling across plugin workspaces while continuing to use plugin theme colors instead of native Unraid button skins.
- UX: Keep long translated button labels wrapping on narrow screens, preserve semantic status colors and minimal folder chevrons, and retain the folder editor's larger 1.3rem tabs with border-only selection highlighting.
- Compatibility: Hard-refresh the Unraid webGUI after updating to load the revised button styles.
