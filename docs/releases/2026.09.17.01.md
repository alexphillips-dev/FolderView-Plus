- UX: Add a direct Edit folder defaults link to Docker and VM settings. Open and change saved defaults, including imported FolderView3 profiles, without creating a source folder or changing existing folders.
- Fix: Keep mouse-hover previews available on computers that also have a touchscreen, while retaining click activation on touch-only devices.
- Fix: Apply Grayscale stopped icons to both icon-and-label and icon-only Docker previews, keeping running members in color unless the separate all-icons grayscale option is enabled.
- Fix: Preserve saved mixed positions of Docker folders and standalone containers after refresh. Folder sorting still orders folders within their available slots; new folders without a saved position initially appear at the top.
- Fix: Make Dashboard card layouts fill their available widget or grid width when Unraid's native app tiles impose fixed widths, including narrow screens and nested folders.
- Reliability: Retry a custom icon upload through the supported inline upload path when the server returns an empty JSON response. Ordinary upload errors are not retried, and an unsuccessful retry still reports the error.
- Localization: Translate the new defaults-editor link across all supported locales and retain localized defaults controls.
