- Quality: Support bundles now include the last persisted Docker-page rendered-state snapshot and a recent Docker bulk-update trace, so Settings-page exports can still show what the Docker UI actually rendered after an update flow.
- Quality: The Docker page now records visible folder/member row action text, expanded state, and missing folder-class counts into a support-bundle snapshot to help isolate stale row repaint issues from correct runtime state.
- Quality: Added regression coverage for the new Docker support-bundle telemetry collectors and their wiring into the Settings diagnostics export path.
