- Fix: Removed the unsupported Native Docker Organizer GraphQL probes and mutations that could only report an unavailable optional integration.
- Diagnostics: Removed the obsolete organizer notice, manual check action, server snapshot metadata, and support-bundle telemetry so health results only describe supported capabilities.
- Reliability: Stopped automatic organizer probes from Docker and Dashboard rendering while preserving FolderView Plus folder behavior and the shared runtime transport used by supported features.
- Maintenance: Added a bounded Settings cleanup for the retired browser status record and a deprecation guard that prevents the removed module, actions, and undocumented mutations from returning.
- Localization: Regenerated all shipped catalogs after removing organizer-only interface text.
