- Fix: Settings and folder-editor saves now preserve unrelated changes made in another tab, including when a save retries after a revision conflict. Delayed branch-pin and Docker start-order saves retain only the intended edits.
- Fix: Settings diagnostics start after Settings initialization and run once, preventing misleading startup errors from accessing settings before they are ready.
- Fix: Overlapping VM actions release abandoned busy indicators while keeping newer and queued actions marked busy. Late responses from superseded actions no longer finalize the current action or trigger a stale page refresh.
- Compatibility: Hard-refresh the Unraid webGUI after updating to load the revised browser scripts.
