- UI/UX: Rebuilt the Advanced Settings Docker start-order workspace to match the approved modern mockup with a descriptive header, live container totals, two-column configuration controls, contextual ownership guidance, and dedicated startup-batch actions.
- UI/UX: Replaced the dense sequence list with a structured preview table showing order, application icon and name, editable wait time, accessible autostart switch, and compact row affordance.
- UI/UX: Reworked disabled-autostart containers into readable action chips and added the next-service-start notice directly beneath the sequence.
- Accessibility: Added responsive desktop, tablet, and mobile layouts, contained table scrolling, visible keyboard focus, reduced-motion handling, and contrast-safe text for the orange primary action.
- Security: Restrict Docker icon rendering to HTTP(S) or root-relative paths, fall back to the Unraid Docker placeholder for unsafe values, and escape persisted container and batch content before rendering.
- Quality: Extracted start-order presentation into a dedicated contracted view module while reducing the mutation workspace controller and the main Settings entrypoint.
- Localization: Regenerated all 27 locale catalogs for the updated start-order surface with zero uncovered UI strings.
- Test: Added focused rendering, unsafe-value, theme-token, architecture, and desktop/mobile browser-fixture coverage for the redesigned workspace.
