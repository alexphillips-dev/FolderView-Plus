- UX: Folder editor tab labels now use 1.3rem text. The selected tab uses an accent border with the same background as other tabs, while keyboard focus remains visible in light and dark themes.
- Test: Add full plugin startup benchmarks for Settings and Docker, covering small through extreme folder libraries, cold and repeat loads, English, German and Arabic, browser blocking, requests, localization and rendering completeness. Existing component benchmarks remain in place.
- Compatibility: Hard-refresh the Unraid webGUI after updating to load the tab styles. Runtime performance optimizations are deferred until the new startup baseline has been reviewed; this update does not claim faster folder loading.
