## FolderView Plus 2026.07.09.12

- Fix: Docker pinned folders now show an explicit thumb-tack badge beside the folder name on the Docker tab, so persisted pinned state is visible after refresh and tab changes.
- Fix: Docker folder row pin indicators now update in place when pin state changes without requiring a full Docker list reload.
