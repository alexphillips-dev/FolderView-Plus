# FolderView Plus 2026.07.19.05

- Test: Add a deterministic, fixture-based browser suite that runs the shipped Docker runtime, privacy, request, settings, folder action, and import modules without requiring a live Unraid server.
- Regression guard: Cover Docker toolbar idempotency and counts, filtering and reset, duplicate-free Host list/FolderView switching, keyboard menus, independent privacy masks, standardized request metadata, and incremental lifecycle reconciliation.
- Regression guard: Cover settings toolbar alignment, accessible folder action behavior, clipboard and focus handling, import operation selection, and import progress reporting.
- Quality: Require the fixture suite for browser-affecting CI changes and run all 36 assertions in Chromium, Firefox, and WebKit during CI, stable release validation, and main-to-dev back-merge validation.
- Diagnostics: Preserve a machine-readable fixture test report and failure screenshots as CI artifacts for faster browser regression troubleshooting.
- Maintenance: Pin the browser test dependencies with a lockfile, key CI caches to that lockfile, and document the local fixture test workflow.
