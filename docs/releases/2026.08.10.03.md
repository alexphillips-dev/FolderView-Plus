## FolderView Plus 2026.08.10.03

This development build restores the `dev` update channel after publishing stable `2026.08.10.02`.

### Release and supply-chain reliability

- Maintenance: Restore dev-channel manifest and package URLs while preserving the complete stable release source.
- Fix: Install Node validation dependencies before release checks and require live Unraid, browser, and theme targets whenever their repository secrets are configured.
- Security: Add a deterministic SHA-256 UUIDv8 serial to the CycloneDX SBOM so GitHub can publish a valid SBOM attestation without weak cryptographic algorithms.
- Quality: Retain the full stable release validation, deterministic package, checksum, provenance, and release-note contracts on the continuing development branch.
