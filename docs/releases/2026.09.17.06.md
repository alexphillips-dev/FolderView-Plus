- Fix: Use the available Docker and VM Dashboard widget width in Classic, Fullwidth, Inset, and Embossed views. Collapsed folders and ungrouped items now wrap across rows, while expanded folders receive a full-width panel with responsive member columns.
- Fix: Remove the single-column member restriction from Fullwidth and improve member spacing in Accordion and other panel views. Long folder names truncate within their tiles while respecting the folder-label visibility setting, and nested folders retain their own expansion state.
- Fix: Stack Compact Matrix cards independently in up to three columns so a tall folder no longer creates empty gaps between cards in adjacent columns. Member columns adapt to their own panel width, including nested panels.
- Diagnostics: Keep Dashboard layout diagnostics aligned with the rendered Compact Matrix columns and panel padding. Hard-refresh the webGUI after updating to load the new layout styles.
