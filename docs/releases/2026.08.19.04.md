- Fix: Stop API container identity differences from recursively scheduling Unraid Docker list reloads and eliminate the resulting 5-7 second Docker-page refresh loop.
- Fix: Stop blank or unreachable container icon URLs from generating repeated requests by using the local fallback immediately and remembering failed sources across later renders for the current page session.
- Reliability: Keep Unraid's native Docker/PHP configuration revision authoritative for structural changes, enrich only matching containers through API-first reads, and preserve native rows while Docker is busy.
- Performance: Replace broad action-followup redraws with incremental runtime reconciliation and remove duplicate deferred support-snapshot scheduling.
- Diagnostics: Add five-session Docker refresh history, privacy-safe reload-source attribution, busy-cycle recovery, API-mismatch aggregates, browser-error aging, and the `FVPLUS-DKR-REFRESH-001` loop verdict.
- Diagnostics: Count completed renders by monotonic generation and keep trace-health timestamps current so exported support bundles accurately describe the observed session.
- Privacy: Keep refresh and API evidence bounded and free of container identities, raw API payloads, GraphQL queries, URLs, tokens, cookies, call stacks, process lists, and syslogs.
- Test: Validate the stable candidate through complete lint, static analysis, unit, workflow, release, install, deterministic-build, browser, theme, responsive, performance, dependency, Git-integrity, and GitHub security checks.
