## 2026.07.17.14

- Localization: Split Portuguese into `pt-BR` and `pt-PT`, identify Simplified Chinese as `zh-Hans`, and prevent Traditional Chinese locales from silently falling back to Simplified Chinese.
- Localization: Generic Portuguese and regional aliases now report the correct known translation status even when English fallback is required.
- Localization: Added namespace-ready catalogs for every registered locale while keeping untranslated files empty and using the English fallback instead of presenting copied English as translation coverage.
- Diagnostics: Added a Localization readiness card with active locale resolution, legacy and modern catalog coverage, current-review status, potentially stale message counts, runtime missing keys, load errors, and the remaining hard-coded UI extraction backlog.
- Support: Added catalog coverage and extraction readiness to sanitized support-bundle localization metadata for faster language and fallback troubleshooting.
- Quality: Enforced one catalog version across legacy and namespace files, required namespace scaffolds, validated regional metadata, and made localization extraction reporting fail when its totals become stale.
- Testing: Added regional fallback regression tests, compact catalog-report tests, placeholder integrity checks, and automated expanded-text and RTL pseudo-locale browser screenshots.
- Performance: Runtime pages now load only the active locale coverage report and skip empty namespace catalogs; the full language matrix is reserved for Settings diagnostics.
- UI/UX: Folder and member selectors now use the active locale's numeric collation so names such as Folder 2 sort before Folder 10.
- Translation workflow: Added Weblate-ready component guidance, a terminology and safety review policy, and a dedicated GitHub translation contribution form.
- Localization: Moved Diagnostics activity, performance, history, repair, theme, retry, and technical-detail text into the English catalog, reducing the tracked hard-coded UI backlog without weakening the regression ceiling.
