- Fix: Start Docker render staging before Unraid's native `loadlist()` paints container rows, preventing orphan dropdown chevrons from appearing one at a time before the completed folder view is revealed.
- Fix: Parse semicolon-separated localization bindings independently so Settings search placeholders, accessibility labels, button text, and titles resolve to their intended translations instead of concatenated localization keys.
- Compatibility: Release Docker render staging when host or isolated Docker views are selected so non-folder views remain visible.
- Quality: Add regression coverage for pre-host-load Docker staging and combined text, placeholder, ARIA-label, and title localization bindings.
