## FolderView Plus 2026.08.11.11

This development build fixes stale Docker folder actions, makes Diagnostics findings actionable, hardens host-theme reconciliation, and prevents successful WebUI popups from being reported as blocked.

### Docker runtime and WebUI actions

- Reliability: Reconcile expanded Docker-folder previews after container start, stop, pause, resume, and restart actions so WebUI, console, and log icons leave their busy state when the lifecycle settles.
- Reliability: Resolve lifecycle state by canonical container name or normalized container ID and perform a targeted preview refresh instead of leaving stale browser state behind.
- Compatibility: Open WebUI tabs through a blank browser-owned handle, isolate `window.opener`, and navigate the accepted handle so browsers that return `null` for `noopener` no longer trigger a false popup-blocked warning.

### Diagnostics and repair guidance

- UX: Turn priority findings into focusable controls that reveal the affected health card, technical details, recommended correction, and repair action.
- Reliability: Require confirmation before supported repairs and explain that a backup is created before orphaned Docker member references or missing custom-icon references are changed.

### Theme consistency

- UI/UX: Add a shared host-theme surface that observes Unraid theme attributes, media-query changes, and extra sample targets across Docker, VM, Dashboard, Settings, and Folder Editor pages.
- Accessibility: Auto-heal low-contrast host accents, expose the resolved editor title accent, clean up observers and media listeners on teardown, and align the Folder Editor top chrome with its dark panel surface.

### Regression coverage

- Quality: Add lifecycle, popup, Diagnostics action, theme observer, load-order, browser-fixture, and architecture-contract coverage for the corrected behavior.
- Security: Refresh the CSP readiness inventory while retaining zero inline events, inline scripts, and style attributes.
