# FolderView Plus 2026.07.27.02

This dev update strengthens release validation, expands automated compatibility coverage, repairs localization completeness, and adds more precise Docker first-load diagnostics.

## Release and scheduled validation

- Reliability: Make release-mode validation fail closed when the Unraid version matrix, browser smoke target, or theme matrix target is missing.
- Reliability: Add a weekly and manually dispatchable workflow that runs deterministic fixtures in Chromium, Firefox, and WebKit.
- Reliability: Run required live Unraid guards, browser smoke, and theme-matrix validation from the scheduled workflow whenever all protected target secrets are configured.
- Maintenance: Update the pinned Playwright development dependency used by browser validation.
- Accessibility: Strengthen light-theme button and navigation contrast after WebKit/Axe identified insufficient contrast in Diagnostics controls.

## Docker diagnostics and architecture

- Diagnostics: Distinguish whole-row movement from action movement within a row during delayed Docker metadata hydration.
- Diagnostics: Record aggregate unavailable WebUI slots, disconnected action targets, and bounded absolute, relative, and row movement without retaining workload identities.
- Maintenance: Extract reusable layout measurement and comparison into a typed, executable Docker runtime module.
- Performance: Remove noisy first-render debug work from the Docker folder hot path and ratchet the main runtime line budget below its previous ceiling.
- Regression guard: Add executable geometry tests, load-order checks, architecture contracts, and coverage for the extracted layout module.

## Quality and localization

- Fix: Add the missing semantic labels for Docker WebUI, console, and log preview actions to every supported catalog.
- Maintenance: Expand c8 coverage enforcement to diagnostics view models, Docker container normalization, Docker geometry, and future-host compatibility.
- Maintenance: Raise PHPStan to level 2 and extend analysis to the validation and API-contract libraries.
- Docs: Record the scheduled/live qualification contract, expanded analysis scope, and privacy-safe layout evidence.
