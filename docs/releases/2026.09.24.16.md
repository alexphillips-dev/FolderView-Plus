- Feature: Add Backup readiness and Live runtime connectivity cards to Settings > Advanced > Diagnostics. The cards show Docker and VM backup age and live connection status.
- Fix: Flag missing, unreadable, empty, or overdue scheduled backups and connections to enabled runtimes that fail. Disabled services and checks without enough evidence appear as informational.
- Compatibility: Hard-refresh the Unraid webGUI after updating to load the new Diagnostics cards and styling.
- Diagnostics: Backup readiness checks snapshot metadata and age without performing a test restore. Runtime connectivity reflects the latest health check.
