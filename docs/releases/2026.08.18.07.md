- Fix: Prevent Docker folder startup from failing when a native or third-party container row omits the expected status or wrapper markup used by single-row previews.
- Reliability: Resolve preview markup from the already-selected source row, validate it before cloning, and fall back to FolderView Plus's model-based preview without stopping later folder members.
- Privacy: Record preview fallback diagnostics using only the numeric preview mode and fixed reason categories; folder and container identities are not included.
- Test: Add real-browser coverage for standard Unraid-style rows, missing status markup, missing wrappers, invalid selectors, state-detail trimming, and continued rendering after malformed members.
