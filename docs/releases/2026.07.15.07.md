### LAN privacy control
- Fix: Replaced the ineffective Mask container IPs setting with a single Mask LAN IPs control wired to the IP portion of the Docker page's LAN IP:PORT column.
- UI/UX: Removed the redundant Mask local IPs checkbox and clarified that Mask ports controls the port portion independently.
- Test: Updated privacy regressions to enforce the LAN-facing label, preference binding, and removal of the duplicate controls.
