- Fix: Export complete environment snapshots containing Docker and VM folders, preferences, defaults, and appearance settings. Reject incomplete export responses instead of reporting a successful empty download, and reconnect environment import previews, confirmation, and refresh after applying.
- Fix: Record successful backup and environment actions in the Activity Center so History and Clear become available after activity. Show the original date and time for persisted recovery events and readable event labels, with technical identifiers available in event details.
- Fix: Keep the latest backup list when Recovery requests overlap or a refresh fails, and avoid rebuilding unchanged recovery controls during refresh.
- Localization: Render Recovery summaries, backup controls, snapshot comparisons, action target text, and performance summaries in the selected language immediately. Standardize current running-state wording across Docker, VMs, editor previews, and appearance controls in all 27 supported languages while preserving stored state values.
- Fix: Keep running preview icons colored when grayscale is limited to stopped members, and avoid reporting a managed-theme update failure when no updates are available.
- UX: Apply the plugin's button styling consistently across Settings, editor dialogs, Docker, VMs, and Dashboard controls. Wrap long translated labels, keep dialog close controls compact, prevent icon-picker and Dashboard option clipping, and add space around Dashboard member icons.
- Compatibility: Hard-refresh the Unraid webGUI after updating to load the revised scripts, styles, and language catalogs. A download-started notice means the file was handed to the browser; check the browser downloads list to confirm it was saved.
