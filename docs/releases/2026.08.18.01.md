- Feature: Add explicit Docker start-order ownership modes, ordered startup batches, per-container delays, remaining-container policy, preview, and guarded autostart persistence.
- UI/UX: Rebuild the Advanced Settings Start Order workspace with live totals, automatic preview hydration, a responsive sequence table, accessible autostart controls, and a collapsible disabled-container group.
- Feature: Replace the single Theme Workspace model with named appearance profiles, global and Docker, VM, or Dashboard layers, scoped previews, inactive imports, and atomic profile actions.
- Feature: Add preview-first folder rule templates for Docker names, images, Compose projects, and labels while preserving manual membership.
- Feature: Add read-only FolderView3 discovery and conversion preview plus a confirmed migration path for folders, rules, defaults, start order, and inactive appearance profiles.
- Reliability: Run environment imports and FolderView3 migrations under one configuration lock with exact snapshots, safety backups, readback verification, and automatic rollback of plugin and Docker autostart state.
- Diagnostics: Add privacy-safe Docker, VM, and Dashboard runtime evidence with bounded page captures and sanitized support-bundle summaries.
- Security: Bound and scan imported FolderView3 custom CSS, omit raw configuration from migration reports, validate Docker icon URLs, escape persisted values, and retain guarded mutation endpoints.
- Accessibility: Keep Start Order text at a readable minimum size and preserve keyboard focus, reduced-motion behavior, contrast-safe actions, and contained mobile overflow.
- Localization: Ship the complete Start Order surface across all 27 supported locales and recognize its runtime translation wrappers and rule-template keys during strict usage validation.
- Fix: Correct lightweight Docker autostart detection, preserve peer-container state during toggles, and remove obsolete Start Order styles left by the responsive redesign.
- Compatibility: Retain full Docker detail-row support and acknowledge Unraid API v4.37.1 after confirming no Docker schema, native-page, or organizer contract changes.
- Reliability: Replace scheduled live-Unraid checks with deterministic isolated fixture validation while keeping live-host qualification available only as an operator-controlled action.
- Docs: Refresh the GitHub presentation and comprehensive user wiki, including the new banner, consistent badges, and an explicitly labeled rolling 14-day clone metric.
- Quality: Enforce smaller browser and PHP ownership boundaries for Start Order, rule templates, appearance profiles, diagnostics, FolderView3 migration, and environment transactions.
- Test: Expand regression, browser-fixture, localization, performance, workflow, release, install, and compatibility-monitor coverage for the complete stable release.
