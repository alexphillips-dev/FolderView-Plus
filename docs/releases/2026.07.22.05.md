- UI/UX: Health summary visibility now controls the current Docker and VM summary cards, Docker-only scoring controls are clearly separated from VM resource thresholds, and the retired Compact card layout option was removed.
- Feature: Selecting Strict, Balanced, or Lenient now applies the preset stopped thresholds, update handling, and all-stopped behavior immediately while preserving later fine-tuning.
- Fix: All stopped mode now honors Warning severity instead of always being overridden by the 100% stopped critical threshold.
- Reliability: Docker and VM runtime summary badges now react to cross-tab preference broadcasts without requiring a page reload.
- Localization: Added the revised health summary and scoring copy across all 18 shipped locales and advanced the catalog to revision 2026.07.22.1.
- Test: Added behavioral coverage for summary visibility, every Docker health scoring control, removed preferences, VM control separation, and live runtime badge application.
