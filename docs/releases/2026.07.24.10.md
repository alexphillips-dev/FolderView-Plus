- Fix: Keep the Docker-page Privacy switch mounted while its optimistic preference is saved and confirmed, preventing the control from visually bouncing back before returning to the selected state.
- Reliability: Synchronize Privacy enabled, pending, cross-tab, menu, and mask-option state in place without recreating Unraid's switch widget or duplicating interaction handlers.
- Test: Add browser lifecycle coverage for delayed saves, repeated reconciliation, external state changes, failed synchronization, stable DOM identity, and single-handler behavior.
