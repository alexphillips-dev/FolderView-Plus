- Fix: Add the complete Docker Start Order message set to the Settings localization catalog so visiting the tab no longer records missing-key diagnostics.
- Localization: Ship the 27 Start Order labels in every supported language while preserving English fallback behavior and complete locale coverage.
- Reliability: Register the Start Order translation wrapper with the localization usage guard so future missing catalog keys fail validation before release.
- Test: Verify every Start Order key exists and is non-empty across all 27 locale catalogs, alongside runtime, browser localization, and responsive Settings coverage.
