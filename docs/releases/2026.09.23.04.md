- Fix: Tree moves now save a folder's parent and manual position together, with revision checks that reject stale changes instead of leaving a partial move.
- UX: Moving a folder inside another folder or back to the root keeps the current sort mode. Before and After moves set Manual sort, with the change shown in the move dialog.
- UX: Drag folders between levels using labeled Before, Inside, and After targets. Review the destination path and number of folders in the moving branch before confirming; the Move folder action remains available for keyboard use.
- UX: Large collapsed Docker previews initially show up to 24 container cards and 12 child folder chips. Use Show more to reveal the rest.
- Compatibility: Hard-refresh the Unraid webGUI after updating to load the revised Settings and Docker assets.
