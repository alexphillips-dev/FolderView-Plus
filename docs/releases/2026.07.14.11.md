## FolderView Plus 2026.07.14.11

- UI/UX: Made managed Auto-Rules the primary full-width Rules workspace for new and existing folders, while hiding empty legacy regex controls.
- Compatibility: Added a compact read-only compatibility card for existing legacy regex values with current-match, advanced-conflict, and overlapping-folder previews.
- Compatibility: Added a guarded one-click conversion that creates a backup, appends an Advanced Include / Name Regex fallback, clears the legacy value only after both stores are written, and restores the original data if either write fails.
- Fix: Aligned the JavaScript and PHP rule engines with the documented global priority contract so the first matching enabled include or exclude rule decides the result.
- Compatibility: Kept legacy regex runtime evaluation, imports, backups, templates, diagnostics, and support-bundle data intact; invalid legacy patterns retain a focused repair path.
- Test: Added regression coverage for migration previews, invalid and whitespace-sensitive expressions, duplicate-rule safety, rollback/backup contracts, advanced-first layout, and matching JS/PHP priority behavior.
