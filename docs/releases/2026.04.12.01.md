- Fix: Docker folder bulk-update reconciliation now stays active across later host Docker page redraws, so member rows and folder badges repaint from live runtime state after the update dialog finishes instead of waiting for a full page refresh.
- Quality: The Docker bulk-update runtime action now listens for post-folder-render rebuild events during a short reconcile window, which closes the race where Unraid redraws the list after FolderView Plus already ran its first in-place sync.
- Regression guard: Added coverage for the post-folder-render reconcile event so Docker bulk-update refreshes stay aligned with FolderView Plus render timing.
