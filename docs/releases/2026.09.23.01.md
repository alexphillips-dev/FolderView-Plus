- Fix: Restoring the latest Docker or VM backup now selects the intended snapshot before creating its safety backup, so it no longer restores the current state by mistake.
- Fix: Restores now recover saved preferences alongside folders, including rules, pins, ordering, and Dashboard layout. Older folder-only snapshots continue to preserve existing preferences.
- Fix: Restoring an older snapshot works when the backup retention limit is reached, including a limit of one. Retention runs after a successful restore and preserves the new safety backup for undo.
- Fix: Restore operations wait for pending preference saves and roll back configuration changes if a restore fails. Confirmation text explains the restored contents in every supported language.
