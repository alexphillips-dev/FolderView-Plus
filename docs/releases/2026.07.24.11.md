- Fix: Restore Docker-page Privacy masking when Unraid's switch widget reports a user change through jQuery instead of a native bubbled change event.
- Reliability: Bind the stable Privacy input directly while retaining single-handler guards and suppressing persistence during programmatic state synchronization.
- Test: Exercise the real jQuery-only switch notification path while preserving optimistic, confirmed, external, failed-save, stable-widget, and duplicate-handler coverage.
