- Diagnostics: Fixed the Settings support-bundle preview so its first refresh immediately merges browser-stored Dashboard layout evidence instead of waiting for a support-bundle export.
- Quality: Added lifecycle regression coverage proving Dashboard visual telemetry is enriched, cached, and available to the preview before its initial render.
