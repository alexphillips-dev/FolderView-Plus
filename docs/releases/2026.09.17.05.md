- Fix: Keep parent and ancestor folders open when expanding nested Docker or VM folders in the Dashboard's Accordion layout. Sibling folders still collapse as expected, and saved expansion states keep nested folders visible after a page refresh.
