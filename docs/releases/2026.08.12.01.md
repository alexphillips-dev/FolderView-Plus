- Maintenance: Synchronized stable source and workflow fixes from main back into dev.
- Quality: Rebuilt the dev package so the shipped archive exactly matches the synchronized source.
- Test: Revalidated the complete dev state without allowing packaged-source drift.
