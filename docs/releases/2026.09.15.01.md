- Fix: Protected save failures now identify missing page security information, nonce preparation failures, rejected Unraid session checks, and proxy origin mismatches, with localized recovery guidance and a copyable support code.
- Diagnostics: Keep a bounded, sanitized history of request failures through navigation to Diagnostics in the same browser tab. Support bundles include the failure phase and reason without submitted values, addresses, raw responses, or credentials.
- Fix: Preserve saved FolderView3 Docker and VM folder order when a valid order snapshot is available. Preview warnings explain invalid snapshots, skipped references, and unassigned items whose native page positions cannot be imported.
- Compatibility: Preserve custom theme page scopes, including styles targeting multiple pages. Disabled themes become separate inactive appearance profiles; previews explain unsupported source metadata and CSS compatibility limits.
- UI/UX: Add Basic Settings shortcuts for Docker folders, VM folders, Appearance, and Recovery, plus color preset previews, undo, and Save as new profile. Presets retain other customizations and apply only after an explicit save.
- Localization: Translate the new guidance and controls across all 27 supported languages, including reviewed color and navigation terminology.
- Compatibility: Request guards remain enabled, imported themes remain inactive until selected, and existing appearance profiles are preserved when saving a copy. Hard-refresh the Unraid webGUI after updating.
