- Fix: Classify initial Docker and VM runtime loading as cold even when it finishes after the Settings interface becomes ready. Older runtime-loading samples with ambiguous classifications are discarded when restoring browser performance history; other timing history is retained. Repeated slow warm loads still produce warnings.
- Localization: Replace fragmented performance-warning text with a complete translated message in all 27 supported languages. Translate the Settings loading subtitle from the first server-rendered frame and retain its explicit translation binding after startup.
- Fix: Stop reporting a visible Settings loading screen as a blank page. Truly blank pages retain their early checks, while a loading screen that remains unfinished for 60 seconds receives a distinct, translated loading-timeout message.
- Compatibility: Hard-refresh the Unraid webGUI after updating to load the revised browser scripts and language catalogs.
