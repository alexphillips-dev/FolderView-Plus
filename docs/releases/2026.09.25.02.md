- Fix: Settings now detects when its content is clipped by an ancestor or only its header remains visible, so the blank-page checks do not report a usable page in those cases.
- Compatibility: Hard-refresh the Unraid webGUI after updating so the revised Settings scripts load.
