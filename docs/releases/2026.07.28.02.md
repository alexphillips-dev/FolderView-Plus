# FolderView Plus 2026.07.28.02

- Security: Build third-party icon results with DOM nodes and text properties instead of appending an HTML string.
- Security: Keep localized rich-text fallback content inert through direct `textContent` assignment.
- Security: Normalize every encoded npm scope marker when generating CycloneDX package URLs.
- Test: Add regression contracts for DOM-only third-party icon rendering and complete SBOM purl normalization.
