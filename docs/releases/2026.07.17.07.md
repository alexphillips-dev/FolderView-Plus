- UX: Docker page startup now keeps a non-interactive snapshot of the native container list visible while FolderView assembles the folder tree, eliminating the blank table between the Docker header and the completed folder view.
- Fix: Removed render staging from Unraid's native `loadlist()` phase so containers can paint normally before FolderView begins its atomic folder conversion.
- Quality: The temporary render snapshot is isolated from user input and stripped of IDs, inline handlers, and operational folder classes so it cannot receive actions or interfere with the hidden live render.
- Quality: Expanded Docker bootstrap, update-flow, layout, and performance regression coverage for the snapshot-backed atomic render path.
