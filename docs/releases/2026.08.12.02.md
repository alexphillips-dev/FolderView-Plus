- Feature: Added explicit Docker start-order ownership modes so Unraid order can remain unmanaged, follow the Docker page, or use custom startup batches.
- Feature: Added Docker autostart sequence controls with ordered batches, per-item waits, remaining-container policy, preview, and guarded persistence.
- Feature: Replaced the single Theme Workspace customization model with named appearance profiles, global and Docker, VM, or Dashboard layers, inactive imports, and scoped preview controls.
- Feature: Added atomic appearance-profile create, rename, activate, layer-save, bulk-update, GitHub-import, and delete actions with readback-safe normalization.
- Feature: Added reusable, preview-first folder rule templates for common Docker name, image, Compose project, and label patterns while preserving manual membership.
- Feature: Added a privacy-safe runtime diagnostics model shared by Docker, VM, and Dashboard, including bounded page captures and sanitized support-bundle summaries.
- Feature: Added read-only FolderView3 installed-config discovery and export preview with compatible folder, rule, defaults, start-order, and inactive appearance-profile conversion.
- Feature: Added confirmed FolderView3 migration apply with source-digest freshness checks; native Docker autostart replacement remains explicitly opt-in.
- Security: FolderView3 custom CSS is bounded and scanned before conversion, preview reports exclude raw target configuration, and migration endpoints use the standard mutation guard.
- Reliability: Environment imports and FolderView3 migrations now run under one configuration lock with global and per-type safety backups, exact file snapshots, readback verification, and automatic rollback of folders, preferences, metadata, theme assets, and Docker autostart state.
- Quality: Split start-order, rule-template, theme-profile, page-diagnostics, FolderView3 preview, FolderView3 apply, and environment-transaction responsibilities into enforceable browser and PHP module boundaries.
- Quality: Expanded generated localization catalogs across all 27 shipped locales for the new workspaces, previews, warnings, and migration states.
- Test: Added regression coverage for start-order modeling, autostart workspace behavior, rule conversion/templates, theme profiles/actions, runtime diagnostics, FolderView3 conversion, mutation confirmation, digest validation, failure injection, and full transaction rollback.
