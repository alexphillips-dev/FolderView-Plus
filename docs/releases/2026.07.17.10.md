- Fix: Host list now explicitly extracts every native container row from FolderView storage, removes plugin folder rows and grouping classes, and restores Unraid's standard Docker table instead of relying on the host refresh to dismantle plugin-owned DOM.
- Compatibility: Host restoration prefers freshly rendered direct Unraid rows when both native and previously stored copies exist, removes duplicates defensively, and falls back safely for containers missing from the saved host order.
- Quality: Added request-bundle telemetry for Host list restoration counts and regression coverage for native row extraction, cleanup, ordering, duplicate handling, and the Host-mode render branch.
