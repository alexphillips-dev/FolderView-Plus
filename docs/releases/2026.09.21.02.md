- Fix: Restore configured minimal folder chevrons and their colors on Docker and VM pages. Preserve colored status and alert chips in Settings and remove the unwanted outer box around pinned switches while keeping plugin button themes.
- Fix: Prevent the folder editor from flashing a startup error while its scripts are still loading normally. Missing or stalled scripts and a missing runtime startup marker still produce actionable errors.
- Performance: Allow versioned editor scripts to use the browser cache on normal folder opens, while explicit recovery retries request fresh copies. Reduce repeated translation work on empty attributes and unchanged, unrecognized page text across all supported languages.
- Compatibility: Hard-refresh the Unraid webGUI after updating. Check folder loading and navigation on your server; automated performance checks use isolated fixtures rather than a live Unraid system.
