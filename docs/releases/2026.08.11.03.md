## FolderView Plus 2026.08.11.03

This development build splits foundational utilities and GraphQL transport into enforceable modules while preserving the existing browser and CommonJS APIs.

### Foundational module boundaries

- Refactor: Move dependency-free scheduling, storage, escaping, identifier, and URL-sanitization helpers into `folderviewplus.utils-foundation.js` while keeping `FolderViewPlusUtils` as the compatibility facade.
- Refactor: Split GraphQL request mechanics, subscription lifecycle management, and typed Docker actions into focused transport child modules while keeping `FolderViewPlusRuntimeTransport` as the public facade.
- Compatibility: Load the new child modules in dependency order across Settings, Docker, VMs, Dashboard, Folder Editor, and deterministic browser fixtures.
- Reliability: Preserve stale-request rejection, abort behavior, bounded subscription reconnect, CSRF headers, capability gating, and privacy-safe aggregate transport diagnostics.

### Enforcement and regression coverage

- Quality: Reduce the utility facade from 2,346 to 2,054 lines and the transport facade from 1,060 to 499 lines, with ratcheted budgets for every new child module.
- Quality: Add explicit architecture contracts, namespace-member ownership, page load-order checks, compatibility-facade identity checks, and package smoke requirements for all extracted scripts.
- Quality: Update Docker transport architecture documentation and CSP readiness metadata for the new module graph.
- Security: Keep CSP readiness at zero inline events, inline scripts, and style attributes while retaining sanitized URL, error, and diagnostic behavior.
