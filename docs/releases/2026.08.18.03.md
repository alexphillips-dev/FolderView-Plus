- Fix: Make the in-plugin update indicator follow the installed manifest channel so dev installations compare against dev while stable installations continue comparing against main.
- Security: Resolve update sources from an explicit dev/main allowlist and fall back to the stable channel when the installed manifest is missing or unrecognized.
- Test: Cover XML entity and Community Applications manifest formats, dev and main resolution, and fail-closed behavior before publishing the follow-up dev update used to exercise the indicator.
