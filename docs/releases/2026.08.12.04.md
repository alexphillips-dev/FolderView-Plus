- Accessibility: Raise every visible Docker start-order label, metric, control, table value, badge, chip, and notice to a minimum computed size of 1.2rem.
- UI/UX: Restore the Autostart disabled container group as a native collapsible disclosure that is closed by default.
- UI/UX: Add a clear disclosure chevron while retaining the existing one-click actions for re-enabling individual containers.
- Test: Verify the 1.2rem minimum in a real browser before and after expansion, confirm the disclosure defaults closed, and preserve desktop and mobile overflow checks.
