- Feature: Use Unraid GraphQL as the preferred Docker runtime-state source on supported hosts while preserving the established PHP snapshot path as an automatic compatibility fallback.
- Reliability: Add a single-owner runtime coordinator with capability-gated full and targeted reads, stable PHP-to-API identity matching, stale-response cancellation, bounded retry cooldowns, structural-change fallback, and lifecycle disposal.
- Compatibility: Keep Unraid's native Docker controls authoritative, enrich only existing FolderView runtime rows, and leave legacy or API-unavailable hosts fully functional without requiring user configuration or credentials.
- Privacy: Expose only aggregate API capability and health evidence in diagnostics; container names, identifiers, query data, tokens, and server error details are not exported.
- Test: Replace live-Unraid validation and encrypted target secrets with deterministic isolated GraphQL profiles covering current, legacy, limited, denied, partial, unavailable, rate-limited, schema-drift, and future-native host behavior.
- Test: Run the shipped API-first path through browser fixtures, responsive themes, lifecycle reconciliation, accessibility checks, architecture contracts, and release/install guards without contacting a live Unraid system.
- Docs: Expand the API integration, runtime architecture, compatibility, prerelease qualification, contributor, and architecture-decision guidance for the API-first rollout and its safe fallback boundaries.
