- Fix: Remove obsolete Docker Start Order styles left behind by the responsive workspace redesign so strict validation no longer reports unused selectors.
- Reliability: Recognize the VM translation wrapper and rule-template localization fields as real key references instead of incorrectly flagging their live catalog entries as unused.
- Test: Revalidate the Start Order workspace in browser fixtures, all supported locale catalogs, runtime performance budgets, release guards, and install smoke coverage.
