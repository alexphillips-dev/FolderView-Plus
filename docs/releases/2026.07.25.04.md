# FolderView Plus 2026.07.25.04

This dev follow-up restores reliable Settings startup when the activity diagnostics workspace initializes.

## Settings reliability

- Fix: Register the activity diagnostics API and module-ready marker before optional workspace rendering so a panel-specific failure cannot be misreported as a missing script.
- Reliability: Make repeated module evaluation idempotent, isolate activity diagnostics declarations, and contain each optional diagnostics startup failure independently.
- Maintenance: Remove an unused timestamp helper exposed by the stricter module scope.
- Regression guard: Execute the shipped activity diagnostics script twice in one browser-like context, verify it owns only one startup handler, and confirm registration precedes optional startup work.
