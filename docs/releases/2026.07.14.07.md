## FolderView Plus 2026.07.14.07

- Fix: Scoped the modern Members-tab six-dot grip through the entire editor form so regular Docker/VM member rows receive the same styling as nested-folder rows.
- Fix: Added explicit dot classes with forced block display and fixed min/max dimensions, preventing host button-span rules from collapsing the dots into a blank handle.
- UI/UX: Strengthened the editor-wide drag-handle reset so borders, outlines, backgrounds, and shadows cannot replace the transparent six-dot grip.
- Test: Expanded regression coverage for the stable editor form anchor, both member-table locations, visible fixed-size dots, and borderless handle chrome.
